<phone:PhoneApplicationPage x:Class="Travlexer.WindowsPhone.Views.MapView"
							xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
							xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
							xmlns:Behaviors="clr-namespace:Codify.WindowsPhone.Behaviors;assembly=Codify.WindowsPhone"
							xmlns:Collections="clr-namespace:Codify.WindowsPhone.Collections;assembly=Codify.WindowsPhone"
							xmlns:Controls="clr-namespace:Travlexer.WindowsPhone.Controls"
							xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
							xmlns:DependencyShell="clr-namespace:Codify.WindowsPhone.DependencyShell;assembly=Codify.WindowsPhone"
							xmlns:ec="clr-namespace:Microsoft.Expression.Interactivity.Core;assembly=Microsoft.Expression.Interactions"
							xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
							xmlns:Icons="clr-namespace:Travlexer.WindowsPhone.Controls.Icons"
							xmlns:Maps="clr-namespace:Microsoft.Phone.Controls.Maps;assembly=Microsoft.Phone.Controls.Maps"
							xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
							xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
							xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
							xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
							xmlns:Triggers="clr-namespace:Codify.WindowsPhone.Triggers;assembly=Codify.WindowsPhone"
							xmlns:ViewModels="clr-namespace:Travlexer.WindowsPhone.ViewModels"
							xmlns:WindowsPhone="clr-namespace:Codify.WindowsPhone;assembly=Codify.WindowsPhone"
							Style="{StaticResource PageDefault}"
							FontFamily="{StaticResource PhoneFontFamilyNormal}"
							FontSize="{StaticResource PhoneFontSizeNormal}"
							Foreground="{StaticResource PhoneForegroundBrush}"
							Orientation="Portrait"
							mc:Ignorable="d"
							d:DesignWidth="480"
							d:DesignHeight="768"
							shell:SystemTray.IsVisible="True"
							shell:SystemTray.Opacity="{StaticResource ShellOpacity}"
							d:DataContext="{Binding MapViewModel, Source={StaticResource DesignTime}}">
	<i:Interaction.Triggers>
		<i:EventTrigger EventName="BackKeyPress">
			<i:InvokeCommandAction Command="{Binding CommandGoToDefaultState, Mode=OneTime}" />
		</i:EventTrigger>
	</i:Interaction.Triggers>
	<shell:SystemTray.ProgressIndicator>
		<shell:ProgressIndicator IsIndeterminate="{Binding IsBusy}"
								 IsVisible="True" />
	</shell:SystemTray.ProgressIndicator>
	<phone:PhoneApplicationPage.DataContext>
		<ViewModels:MapViewModel />
	</phone:PhoneApplicationPage.DataContext>
	<Grid>
		<VisualStateManager.VisualStateGroups>
			<VisualStateGroup x:Name="VisualStates">
				<VisualStateGroup.Transitions>
					<VisualTransition GeneratedDuration="0:0:0.3"
									  To="PushpinSelected">
						<Storyboard>
							<ObjectAnimationUsingKeyFrames Storyboard.TargetName="SelectedPushpinContent"
														   Storyboard.TargetProperty="(PushpinOverlay.VisualState)">
								<DiscreteObjectKeyFrame KeyTime="0:0:0.1">
									<DiscreteObjectKeyFrame.Value>
										<WindowsPhone:ExpansionStates>Expanded</WindowsPhone:ExpansionStates>
									</DiscreteObjectKeyFrame.Value>
								</DiscreteObjectKeyFrame>
							</ObjectAnimationUsingKeyFrames>
							<DoubleAnimation Storyboard.TargetName="SelectedPushpinBackground"
											 Storyboard.TargetProperty="(UIElement.Opacity)"
											 Duration="0:0:0.3"
											 EasingFunction="{StaticResource EasingQuinticOut}"
											 To="1" />
							<ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Visibility)"
														   Storyboard.TargetName="SelectedPushpinLayer">
								<DiscreteObjectKeyFrame KeyTime="0">
									<DiscreteObjectKeyFrame.Value>
										<Visibility>Visible</Visibility>
									</DiscreteObjectKeyFrame.Value>
								</DiscreteObjectKeyFrame>
							</ObjectAnimationUsingKeyFrames>
						</Storyboard>
					</VisualTransition>
					<VisualTransition From="PushpinSelected"
									  GeneratedDuration="0:0:0.2">
						<Storyboard>
							<DoubleAnimation Storyboard.TargetName="SelectedPushpinBackground"
											 Storyboard.TargetProperty="(UIElement.Opacity)"
											 Duration="0:0:0.3"
											 EasingFunction="{StaticResource EasingQuinticOut}" />
							<ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Visibility)"
														   Storyboard.TargetName="SelectedPushpinLayer">
								<DiscreteObjectKeyFrame KeyTime="0:0:0.3">
									<DiscreteObjectKeyFrame.Value>
										<Visibility>Collapsed</Visibility>
									</DiscreteObjectKeyFrame.Value>
								</DiscreteObjectKeyFrame>
							</ObjectAnimationUsingKeyFrames>
						</Storyboard>
					</VisualTransition>
					<VisualTransition To="Search">
						<Storyboard>
							<DoubleAnimation Storyboard.TargetName="SearchBackground"
											 Storyboard.TargetProperty="(UIElement.Opacity)"
											 Duration="0:0:0.3"
											 EasingFunction="{StaticResource EasingQuinticOut}"
											 To="1" />
							<DoubleAnimation Storyboard.TargetName="SearchContentPanel"
											 Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
											 Duration="0:0:0.3"
											 EasingFunction="{StaticResource EasingQuinticOut}"
											 To="33" />
							<ObjectAnimationUsingKeyFrames Storyboard.TargetName="SearchPanel"
														   Storyboard.TargetProperty="(UIElement.Visibility)">
								<DiscreteObjectKeyFrame KeyTime="0">
									<DiscreteObjectKeyFrame.Value>
										<Visibility>Visible</Visibility>
									</DiscreteObjectKeyFrame.Value>
								</DiscreteObjectKeyFrame>
							</ObjectAnimationUsingKeyFrames>
						</Storyboard>
					</VisualTransition>
					<VisualTransition From="Search">
						<Storyboard>
							<DoubleAnimation Storyboard.TargetName="SearchBackground"
											 Storyboard.TargetProperty="(UIElement.Opacity)"
											 Duration="0:0:0.3"
											 EasingFunction="{StaticResource EasingQuinticOut}" />
							<DoubleAnimation Storyboard.TargetName="SearchContentPanel"
											 Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
											 Duration="0:0:0.3"
											 EasingFunction="{StaticResource EasingQuinticOut}" />
							<ObjectAnimationUsingKeyFrames Storyboard.TargetName="SearchPanel"
														   Storyboard.TargetProperty="(UIElement.Visibility)">
								<DiscreteObjectKeyFrame KeyTime="0:0:0.3">
									<DiscreteObjectKeyFrame.Value>
										<Visibility>Collapsed</Visibility>
									</DiscreteObjectKeyFrame.Value>
								</DiscreteObjectKeyFrame>
							</ObjectAnimationUsingKeyFrames>
						</Storyboard>
					</VisualTransition>
					<VisualTransition GeneratedDuration="0"
									  To="Drag">
						<Storyboard>
							<DoubleAnimation Storyboard.TargetName="DragPushpin"
											 Storyboard.TargetProperty="(UIElement.RenderTransform).(CompositeTransform.TranslateY)"
											 Duration="0:0:0.3"
											 EasingFunction="{StaticResource EasingQuinticOut}"
											 To="-90" />
							<ObjectAnimationUsingKeyFrames Storyboard.TargetName="DragPushpin"
														   Storyboard.TargetProperty="(UIElement.Visibility)">
								<DiscreteObjectKeyFrame KeyTime="0">
									<DiscreteObjectKeyFrame.Value>
										<Visibility>Visible</Visibility>
									</DiscreteObjectKeyFrame.Value>
								</DiscreteObjectKeyFrame>
							</ObjectAnimationUsingKeyFrames>
						</Storyboard>
					</VisualTransition>
				</VisualStateGroup.Transitions>
				<VisualState x:Name="Default" />
				<VisualState x:Name="Search">
					<Storyboard>
						<ObjectAnimationUsingKeyFrames Storyboard.TargetName="SearchPanel"
													   Storyboard.TargetProperty="(UIElement.Visibility)">
							<DiscreteObjectKeyFrame KeyTime="0">
								<DiscreteObjectKeyFrame.Value>
									<Visibility>Visible</Visibility>
								</DiscreteObjectKeyFrame.Value>
							</DiscreteObjectKeyFrame>
						</ObjectAnimationUsingKeyFrames>
						<DoubleAnimation Storyboard.TargetName="SearchBackground"
										 Storyboard.TargetProperty="(UIElement.Opacity)"
										 Duration="0"
										 To="1"
										 d:IsOptimized="True" />
						<DoubleAnimation Storyboard.TargetName="SearchContentPanel"
										 Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
										 Duration="0"
										 EasingFunction="{StaticResource EasingQuinticOut}"
										 To="33"
										 d:IsOptimized="True" />
					</Storyboard>
				</VisualState>
				<VisualState x:Name="PushpinSelected">
					<Storyboard>
						<ObjectAnimationUsingKeyFrames Storyboard.TargetName="SelectedPushpinContent"
													   Storyboard.TargetProperty="(PushpinOverlay.VisualState)">
							<DiscreteObjectKeyFrame KeyTime="0">
								<DiscreteObjectKeyFrame.Value>
									<WindowsPhone:ExpansionStates>Expanded</WindowsPhone:ExpansionStates>
								</DiscreteObjectKeyFrame.Value>
							</DiscreteObjectKeyFrame>
						</ObjectAnimationUsingKeyFrames>
						<DoubleAnimation Storyboard.TargetName="SelectedPushpinBackground"
										 Storyboard.TargetProperty="(UIElement.Opacity)"
										 Duration="0"
										 To="1"
										 d:IsOptimized="True" />
						<ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Visibility)"
													   Storyboard.TargetName="SelectedPushpinLayer">
							<DiscreteObjectKeyFrame KeyTime="0">
								<DiscreteObjectKeyFrame.Value>
									<Visibility>Visible</Visibility>
								</DiscreteObjectKeyFrame.Value>
							</DiscreteObjectKeyFrame>
						</ObjectAnimationUsingKeyFrames>
					</Storyboard>
				</VisualState>
				<VisualState x:Name="Drag">
					<Storyboard>
						<DoubleAnimation Storyboard.TargetName="DragPushpin"
										 Storyboard.TargetProperty="(UIElement.RenderTransform).(CompositeTransform.TranslateY)"
										 Duration="0"
										 To="-90"
										 d:IsOptimized="True" />
						<ObjectAnimationUsingKeyFrames Storyboard.TargetName="DragPushpin"
													   Storyboard.TargetProperty="(UIElement.Visibility)">
							<DiscreteObjectKeyFrame KeyTime="0">
								<DiscreteObjectKeyFrame.Value>
									<Visibility>Visible</Visibility>
								</DiscreteObjectKeyFrame.Value>
							</DiscreteObjectKeyFrame>
						</ObjectAnimationUsingKeyFrames>
					</Storyboard>
				</VisualState>
			</VisualStateGroup>
			<VisualStateGroup x:Name="ToolbarStates">
				<VisualStateGroup.Transitions>
					<VisualTransition GeneratedDuration="0" To="Expanded">
						<Storyboard>
							<DoubleAnimation Duration="0:0:0.5" To="180" Storyboard.TargetProperty="(UIElement.Projection).(PlaneProjection.RotationX)" Storyboard.TargetName="ExpandButton">
								<DoubleAnimation.EasingFunction>
									<CircleEase EasingMode="EaseOut"/>
								</DoubleAnimation.EasingFunction>
							</DoubleAnimation>
							<DoubleAnimation Duration="0:0:0.5" To="214" Storyboard.TargetProperty="(FrameworkElement.Height)" Storyboard.TargetName="Toolbar" EasingFunction="{StaticResource EasingQuinticOut}"/>
						</Storyboard>
					</VisualTransition>
					<VisualTransition From="Expanded" GeneratedDuration="0" To="Collapsed">
						<Storyboard>
							<DoubleAnimation Duration="0:0:0.6" Storyboard.TargetProperty="(UIElement.Projection).(PlaneProjection.RotationX)" Storyboard.TargetName="ExpandButton" From="180">
								<DoubleAnimation.EasingFunction>
									<CircleEase EasingMode="EaseOut"/>
								</DoubleAnimation.EasingFunction>
							</DoubleAnimation>
							<ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Visibility)" Storyboard.TargetName="CollapseButton">
								<DiscreteObjectKeyFrame KeyTime="0">
									<DiscreteObjectKeyFrame.Value>
										<Visibility>Collapsed</Visibility>
									</DiscreteObjectKeyFrame.Value>
								</DiscreteObjectKeyFrame>
							</ObjectAnimationUsingKeyFrames>
							<ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Visibility)" Storyboard.TargetName="ExpandButton">
								<DiscreteObjectKeyFrame KeyTime="0">
									<DiscreteObjectKeyFrame.Value>
										<Visibility>Visible</Visibility>
									</DiscreteObjectKeyFrame.Value>
								</DiscreteObjectKeyFrame>
							</ObjectAnimationUsingKeyFrames>
							<DoubleAnimation Duration="0:0:0.5" Storyboard.TargetProperty="(FrameworkElement.Height)" Storyboard.TargetName="Toolbar" EasingFunction="{StaticResource EasingQuinticOut}"/>
						</Storyboard>
					</VisualTransition>
				</VisualStateGroup.Transitions>
				<VisualState x:Name="Collapsed"/>
				<VisualState x:Name="Expanded">
					<Storyboard>
						<DoubleAnimation Duration="0" To="180" Storyboard.TargetProperty="(UIElement.Projection).(PlaneProjection.RotationX)" Storyboard.TargetName="ExpandButton" d:IsOptimized="True"/>
						<ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Visibility)" Storyboard.TargetName="CollapseButton">
							<DiscreteObjectKeyFrame KeyTime="0">
								<DiscreteObjectKeyFrame.Value>
									<Visibility>Visible</Visibility>
								</DiscreteObjectKeyFrame.Value>
							</DiscreteObjectKeyFrame>
						</ObjectAnimationUsingKeyFrames>
						<ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Visibility)" Storyboard.TargetName="ExpandButton">
							<DiscreteObjectKeyFrame KeyTime="0">
								<DiscreteObjectKeyFrame.Value>
									<Visibility>Collapsed</Visibility>
								</DiscreteObjectKeyFrame.Value>
							</DiscreteObjectKeyFrame>
						</ObjectAnimationUsingKeyFrames>
						<DoubleAnimation Duration="0" To="214" Storyboard.TargetProperty="(FrameworkElement.Height)" Storyboard.TargetName="Toolbar" d:IsOptimized="True"/>
					</Storyboard>
				</VisualState>
			</VisualStateGroup>
		</VisualStateManager.VisualStateGroups>
		<Maps:Map x:Name="Map"
				  Center="{Binding Center, Mode=TwoWay}"
				  CopyrightVisibility="Collapsed"
				  CredentialsProvider="AiEEQMxcy4psQQEr7jujXL34vU9268zhPAWJRgDcHNpKdKwyP0TxZRiLOX6j2CMW"
				  LogoVisibility="Collapsed"
				  ZoomLevel="{Binding ZoomLevel, Mode=TwoWay}">
			<i:Interaction.Triggers>
				<i:EventTrigger EventName="MapPan">
					<i:InvokeCommandAction Command="{Binding CommandStopTrackingCurrentLocation, Mode=OneTime}" />
				</i:EventTrigger>
				<i:EventTrigger EventName="DoubleTap">
					<i:InvokeCommandAction Command="{Binding CommandStopTrackingCurrentLocation, Mode=OneTime}" />
				</i:EventTrigger>
				<i:EventTrigger EventName="Loaded">
					<i:InvokeCommandAction Command="{Binding CommandStartGeoWatcher, Mode=OneTime}" />
				</i:EventTrigger>
				<i:EventTrigger EventName="Unloaded">
					<i:InvokeCommandAction Command="{Binding CommandStopGeoWatcher, Mode=OneTime}" />
				</i:EventTrigger>
			</i:Interaction.Triggers>
			<toolkit:GestureService.GestureListener>
				<toolkit:GestureListener PinchStarted="OnMapPinchStarted" />
			</toolkit:GestureService.GestureListener>
			<!--<Maps:MapItemsControl IsHitTestVisible="False"
								  ItemsSource="{Binding Pushpins}">
				<Maps:MapItemsControl.ItemTemplate>
					<DataTemplate>
						<Maps:Pushpin Style="{StaticResource PushpinShadow}"
									  Location="{Binding Data.Location, Converter={StaticResource LocationGeoCoordinateConverter}, Mode=TwoWay}"
									  PositionOrigin="BottomCenter" />
					</DataTemplate>
				</Maps:MapItemsControl.ItemTemplate>
			</Maps:MapItemsControl>-->
			<Maps:Pushpin Location="{Binding CurrentLocation}"
						  Background="Black"
						  PositionOrigin="Center"
						  Visibility="{Binding CurrentLocation, TargetNullValue=Collapsed}"
						  Template="{x:Null}"
						  IsHitTestVisible="{Binding DragPushpin, Converter={StaticResource BooleanConverter}, ConverterParameter=reverse}">
				<i:Interaction.Triggers>
					<Triggers:EventTrigger EventName="Tap"
										   IsHandled="True">
						<i:InvokeCommandAction Command="{Binding CommandAddCurrentPlace, Mode=OneTime}" />
					</Triggers:EventTrigger>
				</i:Interaction.Triggers>
				<Grid>
					<Ellipse Width="40"
							 Height="40"
							 Fill="Transparent" />
					<Icons:CurrentLocation Foreground="Black"
										   Background="Transparent"
										   VerticalAlignment="Center"
										   HorizontalAlignment="Center"
										   Width="30"
										   Height="30" />
				</Grid>
			</Maps:Pushpin>
			<Maps:MapItemsControl Collections:DelayCollection.ItemsSource="{Binding Pushpins}"
								  Collections:DelayCollection.Delay="0:0:0.100">
				<i:Interaction.Behaviors>
					<Behaviors:CancelGestureEventBehavior />
				</i:Interaction.Behaviors>
				<Maps:MapItemsControl.ItemTemplate>
					<DataTemplate>
						<Maps:Pushpin x:Name="Pushpin"
									  Background="{Binding Data.Color, Converter={StaticResource PlaceColorToHexConverter}}"
									  Location="{Binding Data.Location, Converter={StaticResource LocationGeoCoordinateConverter}}"
									  PositionOrigin="BottomCenter"
									  Tag="{Binding Data.IsSearchResult}"
									  Visibility="{Binding Parent.DragPushpin, ConverterParameter=reverse, Converter={StaticResource VisibilityConverter}}">
							<toolkit:GestureService.GestureListener>
								<toolkit:GestureListener DragCompleted="OnPushpinDragCompleted"
														 DragDelta="OnPushpinDragDelta"
														 DragStarted="OnPushpinDragStarted"
														 Hold="OnPushpinHold" />
							</toolkit:GestureService.GestureListener>
							<i:Interaction.Triggers>
								<i:EventTrigger EventName="Tap">
									<i:InvokeCommandAction Command="{Binding Parent.CommandSelectPushpin, Mode=OneTime}"
														   CommandParameter="{Binding}" />
									<i:InvokeCommandAction Command="{Binding Parent.CommandStopTrackingCurrentLocation, Mode=OneTime}" />
								</i:EventTrigger>
								<i:EventTrigger>
									<ec:GoToStateAction StateName="Drop"
														TargetObject="{Binding ElementName=Pushpin}" />
								</i:EventTrigger>
							</i:Interaction.Triggers>
							<ContentPresenter Height="32"
											  Width="32"
											  Content="{Binding Data.Icon, Converter={StaticResource PlaceIconToControlConverter}}" />
						</Maps:Pushpin>
					</DataTemplate>
				</Maps:MapItemsControl.ItemTemplate>
			</Maps:MapItemsControl>
			<Maps:MapLayer x:Name="SelectedPushpinLayer"
						   Visibility="Collapsed">
				<i:Interaction.Behaviors>
					<Behaviors:CancelGestureEventBehavior />
				</i:Interaction.Behaviors>
				<Rectangle x:Name="SelectedPushpinBackground"
						   Fill="{StaticResource BrushSemiTransparentBackground}"
						   IsHitTestVisible="False"
						   Opacity="0" />
				<Maps:Pushpin x:Name="SelectedPushpin"
							  Background="{Binding Data.Color, Converter={StaticResource PlaceColorToHexConverter}}"
							  DataContext="{Binding SelectedPushpin}"
							  Location="{Binding Data.Location, Converter={StaticResource LocationGeoCoordinateConverter}}"
							  PositionOrigin="BottomCenter"
							  RenderTransformOrigin="0.5,0.5"
							  Tag="{Binding Data.IsSearchResult}"
							  Visibility="{Binding TargetNullValue=Collapsed}">
					<Maps:Pushpin.RenderTransform>
						<CompositeTransform />
					</Maps:Pushpin.RenderTransform>
					<i:Interaction.Triggers>
						<i:EventTrigger EventName="Tap">
							<i:InvokeCommandAction Command="{Binding Parent.CommandDeselectPushpin, Mode=OneTime}"
												   CommandParameter="{Binding}" />
						</i:EventTrigger>
						<Triggers:RootTouchTrigger CanTrigger="{Binding Visibility, Converter={StaticResource BooleanConverter}, ElementName=SelectedPushpin}">
							<i:InvokeCommandAction Command="{Binding Parent.CommandDeselectPushpin, Mode=OneTime}"
												   CommandParameter="{Binding}" />
						</Triggers:RootTouchTrigger>
						<i:EventTrigger>
							<ec:GoToStateAction TargetName="SelectedPushpin"
												StateName="Drop"
												UseTransitions="False" />
						</i:EventTrigger>
					</i:Interaction.Triggers>
					<Controls:PushpinContent x:Name="SelectedPushpinContent"
											 Address="{Binding Data.FormattedAddress}"
											 CommandDelete="{Binding Parent.CommandDeletePlace, Mode=OneTime}"
											 CommandDeleteParameter="{Binding}"
											 CommandPinSearchResult="{Binding Parent.CommandPinSearchResult, Mode=OneTime}"
											 CommandPinSearchResultParameter="{Binding}"
											 CommandRefresh="{Binding Parent.CommandUpdatePlace, Mode=OneTime}"
											 CommandRefreshParameter="{Binding}"
											 Icon="{Binding Data.Icon}"
											 IsSearchResult="{Binding Data.IsSearchResult, Mode=TwoWay}"
											 Title="{Binding Data.Name}"
											 VisualState="Collapsed"
											 WorkingState="{Binding Data.DataState}" />
				</Maps:Pushpin>
			</Maps:MapLayer>
			<Maps:Pushpin x:Name="DragPushpin"
						  Background="{Binding Color, Converter={StaticResource PlaceColorToHexConverter}}"
						  DataContext="{Binding DragPushpin.Data}"
						  IsHitTestVisible="False"
						  Location="{Binding Location, Converter={StaticResource LocationGeoCoordinateConverter}}"
						  PositionOrigin="BottomCenter"
						  Visibility="Collapsed">
				<i:Interaction.Triggers>
					<i:EventTrigger>
						<ec:GoToStateAction TargetObject="{Binding ElementName=DragPushpin}"
											StateName="Drop"
											UseTransitions="False" />
					</i:EventTrigger>
				</i:Interaction.Triggers>
				<Maps:Pushpin.RenderTransform>
					<CompositeTransform />
				</Maps:Pushpin.RenderTransform>
				<ContentPresenter Height="32"
								  Width="32"
								  Content="{Binding Icon, Converter={StaticResource PlaceIconToControlConverter}}" />
			</Maps:Pushpin>
		</Maps:Map>
		<Border x:Name="Toolbar" Background="{StaticResource PhoneChromeBrush}" Opacity="{StaticResource ShellOpacity}" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0,0,0" Padding="10" CornerRadius="34" Width="68" Height="98">
			<StackPanel>
				<Controls:ButtonCircle Click="OnCaptureScreen" toolkit:TiltEffect.SuppressTilt="True">
					<Icons:Camera/>
				</Controls:ButtonCircle>
				<Button x:Name="ExpandButton" Style="{StaticResource EmptyButton}" toolkit:TiltEffect.SuppressTilt="True">
					<i:Interaction.Triggers>
						<i:EventTrigger EventName="Click">
							<ec:GoToStateAction StateName="Expanded"/>
						</i:EventTrigger>
					</i:Interaction.Triggers>
					<Button.Projection>
						<PlaneProjection/>
					</Button.Projection>
					<Icons:DownArrow Height="40" Width="20">
						<Icons:DownArrow.Projection>
							<PlaneProjection/>
						</Icons:DownArrow.Projection>
					</Icons:DownArrow>
				</Button>
				<Button x:Name="CollapseButton" Style="{StaticResource EmptyButton}" Visibility="Collapsed" toolkit:TiltEffect.SuppressTilt="True">
					<i:Interaction.Triggers>
						<i:EventTrigger EventName="Click">
							<ec:GoToStateAction StateName="Collapsed"/>
						</i:EventTrigger>
					</i:Interaction.Triggers>
					<Button.Projection>
						<PlaneProjection/>
					</Button.Projection>
					<Icons:UpArrow Height="40" Width="20"/>
				</Button>
				<Controls:ButtonCircle Margin="0,0,0,10" Command="{Binding CommandZoomIn, Mode=OneTime}" toolkit:TiltEffect.SuppressTilt="True">
					<Icons:Plus/>
				</Controls:ButtonCircle>
				<Controls:ButtonCircle Command="{Binding CommandZoomOut, Mode=OneTime}" toolkit:TiltEffect.SuppressTilt="True">
					<Icons:Minus/>
				</Controls:ButtonCircle>
			</StackPanel>
		</Border>
		<Grid x:Name="SearchPanel"
			  Visibility="Collapsed">
			<Rectangle x:Name="SearchBackground"
					   Fill="{StaticResource BrushSemiTransparentBackground}"
					   IsHitTestVisible="False"
					   Opacity="0" />
			<Grid x:Name="SearchContentPanel"
				  Height="52"
				  Background="{StaticResource PhoneChromeBrush}"
				  VerticalAlignment="Top">
				<i:Interaction.Triggers>
					<Triggers:RootTouchTrigger CanTrigger="{Binding Visibility, ElementName=SearchPanel, Converter={StaticResource BooleanConverter}}">
						<i:InvokeCommandAction Command="{Binding CommandGoToDefaultState, Mode=OneTime}" />
					</Triggers:RootTouchTrigger>
				</i:Interaction.Triggers>
				<Grid.RenderTransform>
					<TranslateTransform Y="-52" />
				</Grid.RenderTransform>
				<Grid.ColumnDefinitions>
					<ColumnDefinition />
					<ColumnDefinition Width="Auto" />
				</Grid.ColumnDefinitions>
				<Controls:AutoCompleteBox x:Name="SearchBox"
										  Height="76"
										  Margin="-12"
										  FilterMode="None"
										  ItemsSource="{Binding Suggestions}"
										  MinimumPopulateDelay="500"
										  MinimumPrefixLength="3"
										  SelectedItem="{Binding SelectedSuggestion, Mode=TwoWay}"
										  Text="{Binding SearchInput, Mode=TwoWay}"
										  ValueMemberBinding="{Binding Description}">
					<Controls:AutoCompleteBox.InputScope>
						<InputScope>
							<InputScopeName NameValue="Search" />
						</InputScope>
					</Controls:AutoCompleteBox.InputScope>
					<i:Interaction.Triggers>
						<i:EventTrigger EventName="Populating">
							<i:InvokeCommandAction Command="{Binding CommandGetSuggestions}" />
						</i:EventTrigger>
						<Triggers:KeyPressTrigger CanTrigger="{Binding SearchInput, Converter={StaticResource BooleanConverter}}"
												  Key="Enter">
							<i:InvokeCommandAction Command="{Binding CommandSearch, Mode=OneTime}" />
						</Triggers:KeyPressTrigger>
					</i:Interaction.Triggers>
					<Controls:AutoCompleteBox.ItemTemplate>
						<DataTemplate>
							<TextBlock Margin="{StaticResource PhoneVerticalMargin}"
									   Text="{Binding Description}" />
						</DataTemplate>
					</Controls:AutoCompleteBox.ItemTemplate>
				</Controls:AutoCompleteBox>
				<Button Grid.Column="1"
						Command="{Binding CommandSearch, Mode=OneTime}"
						IsHitTestVisible="{Binding SearchInput, Converter={StaticResource BooleanConverter}}"
						Template="{x:Null}"
						toolkit:TiltEffect.SuppressTilt="True">
					<Border Height="52"
							Width="52"
							Background="{StaticResource PhoneForegroundBrush}">
						<Icons:Search Height="32"
									  Width="32"
									  Foreground="{StaticResource PhoneBackgroundBrush}"
									  HorizontalAlignment="Center"
									  VerticalAlignment="Center" />
					</Border>
				</Button>
			</Grid>
		</Grid>
	</Grid>
	<i:Interaction.Behaviors>
		<DependencyShell:ApplicationBar Opacity="{StaticResource ShellOpacity}">
			<DependencyShell:ApplicationBar.Buttons>
				<DependencyShell:ApplicationBarIconButton Command="{Binding CommandStartTrackingCurrentLocation,Mode=OneTime}"
														  IconUri="/Assets/CurrentLocation.png"
														  IsEnabled="{Binding IsTrackingCurrentLocation, Converter={StaticResource BooleanConverter}, ConverterParameter=reverse}"
														  Text="track" />
				<DependencyShell:ApplicationBarIconButton Command="{Binding CommandGoToSearchState,Mode=OneTime}"
														  IconUri="/Assets/Search.png"
														  Text="search" />
			</DependencyShell:ApplicationBar.Buttons>
			<DependencyShell:ApplicationBar.MenuItems>
				<DependencyShell:ApplicationBarMenuItem Command="{Binding CommandClearSearchResults, Mode=OneTime}"
														Text="clear search" />
			</DependencyShell:ApplicationBar.MenuItems>
		</DependencyShell:ApplicationBar>
	</i:Interaction.Behaviors>
</phone:PhoneApplicationPage>