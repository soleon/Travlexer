<phone:PhoneApplicationPage
							xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
							xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
							xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
							xmlns:Maps="clr-namespace:Microsoft.Phone.Controls.Maps;assembly=Microsoft.Phone.Controls.Maps"
							xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
							xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
							xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
							xmlns:ViewModels="clr-namespace:Travlexer.WindowsPhone.ViewModels"
							xmlns:Controls="clr-namespace:Travlexer.WindowsPhone.Controls"
							x:Class="Travlexer.WindowsPhone.Views.MapView"
							FontFamily="{StaticResource PhoneFontFamilyNormal}"
							FontSize="{StaticResource PhoneFontSizeNormal}"
							Foreground="{StaticResource PhoneForegroundBrush}"
							Orientation="Portrait"
							mc:Ignorable="d"
							d:DesignWidth="480"
							d:DesignHeight="800"
							shell:SystemTray.IsVisible="True"
							d:DataContext="{Binding MapViewModel, Source={StaticResource DesignTime}}">
	<phone:PhoneApplicationPage.DataContext>
		<ViewModels:MapViewModel />
	</phone:PhoneApplicationPage.DataContext>
	<Grid>
	<Maps:Map>
		<Maps:MapItemsControl ItemsSource="{Binding UserPins}">
			<Maps:MapItemsControl.ItemTemplate>
				<DataTemplate>
					<Maps:Pushpin Location="{Binding Location, Converter={StaticResource LocationGeoCoordinateConverter}, Mode=TwoWay}"
								  Background="{Binding Color, Converter={StaticResource PlaceColorToHexConverter}}"
								  PositionOrigin="BottomCenter">
						<Controls:PushpinContent Title="{Binding Name}"
									  Address="{Binding FormattedAddress}"
									  Icon="{Binding Icon}" />
					</Maps:Pushpin>
				</DataTemplate>
			</Maps:MapItemsControl.ItemTemplate>
		</Maps:MapItemsControl>
	</Maps:Map>
	</Grid>
</phone:PhoneApplicationPage>