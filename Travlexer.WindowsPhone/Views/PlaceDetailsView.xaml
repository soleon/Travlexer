<phone:PhoneApplicationPage xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
							xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
							xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
							xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
							xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
							xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
							xmlns:ShellExtension="clr-namespace:Codify.WindowsPhone.ShellExtension;assembly=Codify.WindowsPhone"
                            xmlns:controls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls"
                            xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
                            xmlns:ViewModels="clr-namespace:Travlexer.WindowsPhone.ViewModels"
                            x:Class="Travlexer.WindowsPhone.Views.PlaceDetailsView"
							FontFamily="{StaticResource PhoneFontFamilyNormal}"
							FontSize="{StaticResource PhoneFontSizeNormal}"
							Foreground="{StaticResource PhoneForegroundBrush}"
							Style="{StaticResource PageDefault}"
							Orientation="Portrait"
							mc:Ignorable="d"
							d:DesignHeight="768"
							d:DesignWidth="480"
							shell:SystemTray.Opacity="{StaticResource ShellOpacity}"
							shell:SystemTray.IsVisible="True"
							d:DataContext="{Binding PlaceDetailsViewModel, Source={StaticResource DesignTime}}">
	<phone:PhoneApplicationPage.DataContext>
		<ViewModels:PlaceDetailsViewModel />
	</phone:PhoneApplicationPage.DataContext>
	<ShellExtension:ApplicationBar.ApplicationBar>
		<ShellExtension:ApplicationBar Opacity="{StaticResource ShellOpacity}" />
	</ShellExtension:ApplicationBar.ApplicationBar>
	<controls:Pivot>
		<controls:PivotItem Header="place details">
			<ScrollViewer>
				<Grid DataContext="{Binding Data}">
					<Grid.RowDefinitions>
						<RowDefinition Height="Auto"/>
						<RowDefinition Height="Auto"/>
						<RowDefinition Height="Auto"/>
						<RowDefinition Height="Auto"/>
						<RowDefinition Height="Auto"/>
						<RowDefinition Height="Auto"/>
					</Grid.RowDefinitions>
					<TextBox TextWrapping="Wrap" d:LayoutOverrides="Height" Text="{Binding Name, Mode=TwoWay}"/>
					<TextBox TextWrapping="Wrap" d:LayoutOverrides="Height" Grid.Row="1" Text="{Binding Address, Mode=TwoWay}"/>
					<TextBox TextWrapping="Wrap" d:LayoutOverrides="Height" Grid.Row="2" Text="{Binding Location, Mode=TwoWay}"/>
					<toolkit:ListPicker d:LayoutOverrides="Height" Grid.Row="3"/>
					<toolkit:ListPicker d:LayoutOverrides="Height" Grid.Row="4"/>
					<TextBox x:Name="NotesTextBox" TextWrapping="Wrap" Grid.Row="5" AcceptsReturn="True" Height="220" Text="{Binding Notes, Mode=TwoWay}" InputScope="Text"/>
					<TextBlock Text="Notes..." Grid.Row="5" Foreground="{StaticResource PhoneTextBoxReadOnlyBrush}" FontStyle="Italic" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="17,23,0,0" IsHitTestVisible="False" Visibility="{Binding Text, ConverterParameter=reverse, Converter={StaticResource VisibilityConverter}, Mode=OneWay, ElementName=NotesTextBox}"/>
				</Grid>
			</ScrollViewer>
		</controls:PivotItem>
		<controls:PivotItem Header="nearby"/>
	</controls:Pivot>
</phone:PhoneApplicationPage>
