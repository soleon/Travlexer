<phone:PhoneApplicationPage xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
							xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
							xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
							xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
							xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
							xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
							xmlns:ShellExtension="clr-namespace:Codify.WindowsPhone.ShellExtension;assembly=Codify.WindowsPhone"
                            xmlns:controls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls"
                            xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
                            x:Class="Travlexer.WindowsPhone.Views.PlaceDetailsView"
							FontFamily="{StaticResource PhoneFontFamilyNormal}"
							FontSize="{StaticResource PhoneFontSizeNormal}"
							Foreground="{StaticResource PhoneForegroundBrush}"
							Style="{StaticResource PageDefault}"
							Orientation="Portrait"
							mc:Ignorable="d"
							d:DesignHeight="768"
							d:DesignWidth="480"
							shell:SystemTray.Opacity="{StaticResource ShellOpacity}"
							shell:SystemTray.IsVisible="True"
							d:DataContext="{Binding PlaceDetailsViewModel, Source={StaticResource DesignTime}}">
	<ShellExtension:ApplicationBar.ApplicationBar>
		<ShellExtension:ApplicationBar Opacity="{StaticResource ShellOpacity}" />
	</ShellExtension:ApplicationBar.ApplicationBar>
	<controls:Pivot>
		<controls:PivotItem Header="information">
			<ScrollViewer>
				<StackPanel DataContext="{Binding Data}">
					<TextBox TextWrapping="Wrap" Text="{Binding Address, Mode=TwoWay}" Width="456"/>
					<TextBox TextWrapping="Wrap" Text="{Binding Location, Mode=TwoWay}" Width="456"/>
					<HyperlinkButton Content="{Binding WebSite, TargetNullValue=No website found}" Margin="{StaticResource PhoneMargin}" IsEnabled="{Binding WebSite, Converter={StaticResource BooleanConverter}}"/>
					<HyperlinkButton Content="{Binding ContactNumber, TargetNullValue=No contact number found}" Margin="{StaticResource PhoneMargin}" IsEnabled="{Binding ContactNumber, Converter={StaticResource BooleanConverter}}"/>
					<TextBox Text="{Binding Rating, StringFormat=Rating: \{0\}, TargetNullValue=No rating found}" IsEnabled="{Binding Rating, Converter={StaticResource BooleanConverter}}"/>
				</StackPanel>
			</ScrollViewer>
		</controls:PivotItem>
		<controls:PivotItem Header="personalize">
			<StackPanel DataContext="{Binding Data}">
				<TextBox TextWrapping="Wrap" Text="{Binding Name, Mode=TwoWay}"/>
				<toolkit:ListPicker Height="60" Margin="12,0"/>
				<toolkit:ListPicker Height="60" Margin="12,0"/>
				<TextBox x:Name="NotesTextBox" TextWrapping="Wrap" AcceptsReturn="True" Height="220" Text="{Binding Notes, Mode=TwoWay}" InputScope="Text" VerticalAlignment="Top" d:LayoutOverrides="Height"/>
				<TextBlock Text="Notes..." Foreground="{StaticResource PhoneTextBoxReadOnlyBrush}" FontStyle="Italic" HorizontalAlignment="Left" IsHitTestVisible="False" Visibility="{Binding Text, ConverterParameter=reverse, Converter={StaticResource VisibilityConverter}, ElementName=NotesTextBox, Mode=OneWay}"/>
			</StackPanel>
		</controls:PivotItem>
		<controls:PivotItem Header="nearby"/>
	</controls:Pivot>
</phone:PhoneApplicationPage>
